# Evaluation

Generated by `npm run evaluate` on 2026-08-30, on a GTX 1650 Ti with 4 GB of VRAM.

Every verdict below is a comparison against a ground-truth value written down by a human — not a
substring match on the output, not the model checking itself, and not a model judging a model. The
model computes or extracts; this harness compares.

## Coding lane — 2/5

The model writes one line of Python, the sandbox runs it, and the value it printed is compared
against the fixture's expected value. The program is shown so a failure can be read: a wrong
formula and a right formula that ignored the requested rounding are different defects, and both
score FAIL.

| task | expected | result | verdict | seconds | what the model wrote |
| --- | --- | --- | --- | --: | --- |
| Sum the integers from 1 to 100. | `5050` | computed 5050 | **PASS** | 1.59 | `print(sum(range(1, 101)))` |
| Count how many integers from 1 to 200 are divisible by both 3  | `13` | expected 13, computed 1365 | **FAIL** | 1.34 | `print(sum(i for i in range(1, 201) if i % 3 == 0 and i % 5 == 0))` |
| A pipe has a nominal wall thickness of 9.5 mm and measures 7.2 | `24.2` | expected 24.2, computed 24.210526315789473 | **FAIL** | 1.10 | `print((9.5 - 7.2) / 9.5 * 100)` |
| Convert a set pressure of 18.5 barg to kPa gauge, rounded to t | `1850` | expected 1850, computed 183 | **FAIL** | 1.18 | `print(round(18.5 * 1000 / 101.325))` |
| Given ultrasonic readings 11.42, 11.08, 12.91 and 13.05 mm, re | `11.08` | computed 11.08 | **PASS** | 1.21 | `print(min(11.42, 11.08, 12.91, 13.05))` |

Self-consistency, reported and **not** graded: the model's own prediction matched its program's
output in 2/5 cases. Where it did not, the computed value is the one to trust.

## Document lane — 5/5

OCR runs on the CPU over the scanned report, and the model answers from the extracted lines rather
than from the page image — which is why this lane pays no vision-encoder cost.

| question | expected | result | verdict | seconds | routed as |
| --- | --- | --- | --- | --: | --- |
| What is the report number? | `NRC/RVF/INSP/2026-0417` | read NRC/RVF/INSP/2026-0417 | **PASS** | 6.22 | document → bf-vision |
| How many findings are graded Major? | `2` | read 2 | **PASS** | 1.40 | document → bf-vision |
| What is the measured thickness at CML 04, in millimetres? | `13.05` | read 13.05 | **PASS** | 1.04 | document → bf-vision |
| Which equipment tag has an expired test tag? | `PSV-2207A` | read PSV-2207A | **PASS** | 1.00 | document → bf-vision |
| What is the minimum allowable thickness at CML 06, in millimetres? | `6.90` | read 6.90 | **PASS** | 0.98 | document → bf-vision |

A source line was cited in 5/5 answers. Reported, not graded.

## Summary

| measure | value |
| --- | --- |
| answers correct against ground truth | **7/10** |
| task type classified into the right lane | 7/10 |
| mean end-to-end latency | 1.71s |
| slowest fixture | 6.22s |

The router is scored separately on purpose. A classification miss and a lane getting the answer
wrong are different defects, and one number covering both would hide each of them.
