name: Code
description: Full workbench capability with tools exposed through the Code Mode SDK, so the model can combine multi-step operations in one TypeScript program. No network-capable tool.
order: 4
