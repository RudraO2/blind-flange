# Dependencies of services/ingestion/proof/ only — the Story 4.2 timeboxed proof script,
# not the ingestion service's own runtime dependencies (Story 4.3 pins those separately).
#
# Licences verified from the LICENSE file at these exact versions and recorded in
# ../LICENCES.md, per docs/licence-policy.md.
#
# The Tesseract binary itself is not a pip package — installed via
# `winget install tesseract-ocr.tesseract` (5.5.3.20260724, Apache-2.0), recorded separately.

pytesseract==0.3.13
psutil==7.2.2

# Pulled in transitively by pytesseract, already flagged as an open licence question by
# Story 4.1 (reportlab pulls it in too) — see ../LICENCES.md, "The open decision".
pillow==11.3.0
